type Characters = Array<string>;

export type SerialDataType = {
  episode_id: number,
  title: string,
  season: string,
  air_date: string,
  characters: Characters,
  episode: string,
  series: string,
};
